# ============================================================
# requirements-dev.in — tool di sviluppo e QA (NON in produzione)
#
# Per aggiornare:
#   pip-compile --generate-hashes --allow-unsafe -o requirements-dev.txt requirements-dev.in
#
# Installazione locale:
#   pip install -r requirements-dev.txt
# ============================================================

-r requirements.in     # include tutte le dipendenze di produzione

# --- Test ---
coverage>=7.2.0
pytest>=7.3.0
pytest-django>=4.5.0

# --- Security audit ---
bandit>=1.7.5

# --- Dependency management ---
pip-tools>=7.0.0
